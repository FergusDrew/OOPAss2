#include "ResultsMenu.h"

ResultsMenu::ResultsMenu(const std::string& title, Application* app) : Menu(title, app)
{
	Paint();
}

void ResultsMenu::OutputOptions()
{
	//for (int i = 0; i < app->GetStore().SearchName().length(); i++)
}

bool ResultsMenu::HandleChoice(char choice)
{


	return false;
}